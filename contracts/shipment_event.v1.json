{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "swiftlogix/shipment_event/v1",
  "title": "ShipmentEvent",
  "description": "Canonical SwiftLogix shipment event contract. Closed at the top level so that an upstream field rename surfaces as a validation error within one buffer window rather than as silent nulls.",
  "type": "object",
  "additionalProperties": false,
  "required": ["event_id", "event_type", "event_ts", "shipment_id", "source"],
  "properties": {
    "event_id": {
      "type": "string",
      "minLength": 8,
      "description": "Idempotency key. Producer-assigned, stable across at-least-once redelivery."
    },
    "event_type": {
      "type": "string",
      "enum": ["gps_ping", "temperature", "status", "hub_scan"]
    },
    "event_ts": {
      "type": "string",
      "description": "ISO-8601 UTC instant the event occurred at the edge."
    },
    "shipment_id": { "type": "string", "minLength": 4 },
    "vehicle_id": { "type": ["string", "null"] },
    "hub_code": { "type": ["string", "null"] },
    "source": { "type": "string" },
    "status": {
      "type": ["string", "null"],
      "enum": ["pickup", "in_transit", "exception", "delivered", null]
    },
    "geo": {
      "type": ["object", "null"],
      "additionalProperties": false,
      "required": ["lat", "lon"],
      "properties": {
        "lat": { "type": "number", "minimum": -90, "maximum": 90 },
        "lon": { "type": "number", "minimum": -180, "maximum": 180 }
      }
    },
    "temperature_c": { "type": ["number", "null"], "minimum": -80, "maximum": 80 },
    "delivery_window": {
      "type": ["object", "null"],
      "additionalProperties": false,
      "required": ["start", "end"],
      "description": "The block that broke in the 2025 incident. Closed on purpose.",
      "properties": {
        "start": { "type": "string" },
        "end": { "type": "string" }
      }
    },
    "metadata": {
      "type": ["object", "null"],
      "additionalProperties": true,
      "description": "Deliberately open. This is the VARIANT / schema-on-read escape hatch: source-specific keys land here and are never allowed to break the typed columns."
    }
  }
}
